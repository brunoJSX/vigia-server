# State Machine

Gerar apenas quando existir lifecycle explícito.

Exemplos:

* Incident
* Subscription
* Billing

Não gerar state machines para conceitos sem estado próprio.

```mermaid
stateDiagram-v2
```
